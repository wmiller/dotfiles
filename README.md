# Basic usage
On linux or macOS, install stow, then from the base directory, run `stow common`. Run `WindowsConfigs.ps1` in PowerShell on Windowsi.

# Windows
The `WindowsConfigs.ps1` script assumes the dotfiles project is synced to `~/dotfiles`.
